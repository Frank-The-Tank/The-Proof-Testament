import { BibleFilterPipe } from './bible-filter.pipe';

describe('BibleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BibleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
